apiVersion: v1
kind: ServiceAccount
metadata:
  name: skin-lesion-backend
  namespace: skin-lesion-staging
  annotations:
    # Replace using: terraform output backend_workload_role_arn
    eks.amazonaws.com/role-arn: "REPLACE_WITH_TERRAFORM_OUTPUT_backend_workload_role_arn"
