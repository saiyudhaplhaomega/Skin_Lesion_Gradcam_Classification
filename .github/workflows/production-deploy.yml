# Disabled: backend deployment automation lives in
# Skin_Lesion_Classification_backend/.github/workflows/ because the backend
# source is never included when this parent repository is checked out.
name: Disabled Production Deploy Pointer

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run the backend repository's Production Deploy workflow instead."
